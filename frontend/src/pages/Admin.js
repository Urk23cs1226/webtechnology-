import React, { useEffect, useState } from 'react'; import API from '../api/api'; export default function Admin({ user }){ const [stats,setStats]=useState(null); useEffect(()=>{ API.get('/admin/stats').then(r=>setStats(r.data)).catch(e=>{console.error(e);}); },[]); if(!user || user.role!=='admin') return <div><h2>Admin</h2><p>Access denied.</p></div>; return (<div><h2>Admin Panel</h2><pre>{JSON.stringify(stats,null,2)}</pre></div>); }