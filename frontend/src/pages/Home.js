import React, { useEffect, useState } from 'react'; import API from '../api/api'; export default function Home(){ const [wastes,setWastes]=useState([]); useEffect(()=>{ API.get('/waste').then(r=>setWastes(r.data)).catch(()=>{}); },[]); return (<div><h2>Welcome to EcoCycle</h2><p>Track waste, recycle, and support eco products.</p><h3>Recent Collections</h3><div>{wastes.map(w=>(<div key={w._id} className='list-item'><strong>{w.type}</strong> — {w.quantity}kg @ {w.location} by {w.user? w.user.name: 'anon'}</div>))}</div></div>); }